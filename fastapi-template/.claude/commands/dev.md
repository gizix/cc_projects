---
description: Start the FastAPI development server with auto-reload
---

Start the FastAPI development server using uvicorn with auto-reload enabled.

Run the following command:

```bash
uv run fastapi dev
```

This will start the server on http://localhost:8000 with:
- Auto-reload on file changes
- Interactive API documentation at /docs
- Alternative API docs at /redoc
