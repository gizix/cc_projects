---
description: List credential names and metadata (never values)
---

Display all stored credential names and their metadata. Values are never shown.

```bash
uv run agent credential list
```

**Output columns:** Name, Description, Last Updated

**To add a credential:** `/add-credential <name>`
